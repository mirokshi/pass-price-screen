{
  "name": "pass-price-screen",
  "prodcutName": "PasSolutions price sreen",
  "version": "0.5.0",
  "description": "Simple screen showing the price of an item searched by barcode",
  "main": "src/index.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . pass-price-screen --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Apliacion de precios - PASS\""
  },
  "author": "Mrksh",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.6.1",
    "mssql": "^8.1.2",
    "quagga": "^0.12.1"
  },
  "devDependencies": {
    "electron": "^20.1.0",
    "electron-packager": "^16.0.0",
    "electron-reload": "^2.0.0-alpha.1"
  }
}
